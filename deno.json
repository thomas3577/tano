{
  "tasks": {
    "cache": "deno cache --lock=deno.lock --lock-write ./mod.ts",
    "test": "deno test --unstable-kv --allow-read --allow-run --allow-env --allow-write",
    "run": "cd ./examples && deno run --unstable-kv --allow-read --allow-run --allow-env --allow-write ../tano.ts --file ./tanofile.ts"
  },
  "compilerOptions": {
    "strict": true
  },
  "imports": {
    "$std/": "https://deno.land/std@0.213.0/"
  },
  "lint": {
    "rules": {
      "tags": [
        "recommended"
      ],
      "include": [
        "ban-untagged-todo"
      ],
      "exclude": [
        "no-explicit-any",
        "no-inferrable-types"
      ]
    }
  },
  "fmt": {
    "useTabs": false,
    "lineWidth": 360,
    "indentWidth": 2,
    "singleQuote": true,
    "proseWrap": "preserve",
    "exclude": [
      ".vscode/**",
      ".tano/**",
      "coverage/**",
      "cspell.json"
    ]
  }
}
