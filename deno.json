{
  "tasks": {
    "install": "deno install -A -f -n  tano --config ./deno.json  ./tano.ts",
    "run": "cd ./examples && deno run --allow-read --allow-run --allow-env ../tano.ts --file ./tanofile.ts",
    "check": "deno fmt --check && deno lint",
    "test": "deno test --allow-read --allow-run --allow-env"
  },
  "compilerOptions": {
    "strict": true
  },
  "imports": {
    "std/": "https://deno.land/std@0.177.0/"
  },
  "lint": {
    "rules": {
      "tags": [
        "recommended"
      ],
      "include": [
        "ban-untagged-todo"
      ],
      "exclude": [
        "no-explicit-any",
        "no-inferrable-types"
      ]
    }
  },
  "fmt": {
    "files": {
      "exclude": [".vscode/"]
    },
    "options": {
      "useTabs": false,
      "lineWidth": 240,
      "indentWidth": 2,
      "singleQuote": true,
      "proseWrap": "preserve"
    }
  }
}
